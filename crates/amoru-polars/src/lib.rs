//! Amoru engine bridge: hosts a kernel inside Polars; depends on `amoru-kernel` and `polars` only (feature `polars`).
//!
//! Design: `architecture/sdd/01-contracts.md (section a) and architecture/sdd/05-adapters.md`. This is the wave 0 stub (F0.1); the crate is
//! built in post wave 1, by its own brief.
