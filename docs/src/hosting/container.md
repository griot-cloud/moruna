# A cgroup v2 container or Kubernetes pod

Placeholder. Filled by F7.5; cites `architecture/moruna-runtime-design.md` section 6 (a pod or container with cgroup v2 limits), `architecture/sdd/03-discovery.md` sections f and h (`memory.high`, `memory.max`, `cpu.max`), `architecture/sdd/06-reactor.md` section h (the io_uring seccomp fallback), and `architecture/sdd/00-preamble.md` section 5 rows `budget.host`, `budget.disk` and `staging.dir`.
