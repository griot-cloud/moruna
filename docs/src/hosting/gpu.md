# A GPU host

Placeholder. Filled by F7.5; cites `architecture/moruna-runtime-design.md` section 6 (a GPU host), `architecture/sdd/00-preamble.md` section 5 rows `budget.device`, `arena.pin` and `arena.huge_pages`, and section 7 item E1 (no GPU host exists, so this page is described, not verified).
