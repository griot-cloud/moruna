# Run inside a container with a budget

Placeholder. Filled by F7.7; cites `architecture/amoru-runtime-design.md` section 6 (a pod or container with cgroup v2 limits), `architecture/sdd/00-preamble.md` section 5 row `budget.host`, and section 6.6 (the container gate, `--memory` and `--cpus`).
