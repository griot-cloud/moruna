# Budgets and the environment variables

Placeholder. Filled by F7.2; cites `architecture/sdd/00-preamble.md` section 5 rows `budget.host`, `budget.device`, `budget.disk`, `budget.reserve_fraction`, `staging.dir` and `host_profile` (the `MORUNA_BUDGET`, `MORUNA_SPILL_DIR`, `MORUNA_SPILL_LIMIT` and `MORUNA_HOST_PROFILE` variables with their owners), and `architecture/sdd/03-discovery.md` section f.
